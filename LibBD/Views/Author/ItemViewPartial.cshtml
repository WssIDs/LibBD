@model LibDB.DAL.Author
@*<div class="col-md-12">*@
    <!-- BEGIN Portlet PORTLET-->
    @*<div class="portlet">
        <div class="portlet-title">
            <div class="caption caption-green">
                <span class="caption-subject text-uppercase">@Model.AuthorId. @Model.CardTitle</span>
            </div>
            <div class="actions">
                <label class="btn grey-salsa active">
                    @Model.CardYear
                </label>
            </div>
        </div>
        <div class="portlet-body">
            <h5><strong>Автор:</strong> @Model.LastName @Model.FirstName</h5>
            <p><strong>Соавторы:</strong> @Model.Collaborators</p>
            <p><strong>Название:</strong> @Model.Title</p>
            <p><strong>Год:</strong> @Model.Year</p>
            <p><strong>Номер:</strong> @Model.Number</p>
            <p><strong>Страницы:</strong> @Model.Pages стр.</p>
            <p>@Model.Description</p>
        </div>
        <div class="portlet-body">
            <hr />
            <p><strong>@Model.LastName, @Model.FirstName.First().</strong></p>
            <p>@Model.CardTitle [Текст] / @Model.Collaborators // @Model.Title. - @Model.Year. - @Model.Number. - C. @Model.Pages.</p>
            <p>@Model.Description</p>
        </div>
    </div>*@
<div class="card mb-5">
    <div class="card-header text-uppercase text-light bg-primary">
        <span class="badge badge-light float-left m-1">@Model.AuthorId</span> @Model.CardTitle <span class="badge badge-dark float-right m-1">@Model.CardYear</span>
</div>
    <div class="card-body">
        <h6 class="card-title">@Model.LastName @Model.FirstName</h6>
    </div>
    <ul class="list-group list-group-flush list-inline">
        <li class="list-group-item">@Model.Collaborators</li>
        <li class="list-group-item">@Model.Title</li>
        <li class="list-group-item">@Model.Year</li>
        <li class="list-group-item">@Model.Number</li>
        <li class="list-group-item">@Model.Pages стр.</li>
        <li class="list-group-item">@Model.Description</li>
    </ul>
    <div class="card-footer text-muted">
        <p><strong>@Model.LastName, @Model.FirstName.First().</strong></p>
        <p>@Model.CardTitle [Текст] / @Model.Collaborators // @Model.Title. - @Model.Year. - @Model.Number. - C. @Model.Pages.</p>
        <p>@Model.Description</p>
    </div>
</div>
    <!-- END Portlet PORTLET-->
    @*</div>*@

